import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,

} from 'react-native';
import React from 'react'

const Card = () => {
    return (

        <View style={styles.container}>
            <Text>t</Text>
        </View>

    )
}

// IntelligentRobot

export default Card

const styles = StyleSheet.create({
    container: {
        flex: 1,

    },

});